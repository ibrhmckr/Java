import java.awt.Color;

import javax.swing.ImageIcon;
import javax.swing.JFrame;

public class gui {
	
	public static void main(String[] args) {
		
		/*
		
		JFrame frame = new JFrame();// Creates a frame
		frame.setTitle("My frame");// sets the title of the frame
		frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE); // exits out of application
		frame.setResizable(false);//prevent frame being resized
		frame.setSize(420,420); // sets the x-dimenstion and 
		frame.setVisible(true);
		
		ImageIcon image = new ImageIcon("icon.png"); // Creates an ImageIcon
		frame.setIconImage(image.getImage()); // Change icon of the frame
		
		frame.getContentPane().setBackground(new Color(0,180,250)); // Change color of the background // Hexa and RGB values takes
	 	*/
		myFrame frame = new myFrame(); // Crates an instance of myFrame
		
	   // new myFrame();  // --> Works as well
		
		
	}

}
