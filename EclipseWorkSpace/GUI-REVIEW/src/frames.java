import java.awt.Color;
import javax.swing.ImageIcon;
import javax.swing.JFrame;
public class frames {

	public static void main(String[] args) {
		/*
		//JFrame = a GUI window to add component to
		JFrame frame = new JFrame(); // creates a frame
		frame.setSize(420,420);//sets x dimension, and y-dimension of frame
		frame.setTitle("Reiew Title"); //sets the title of frame
		frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE); //exit out of application
		frame.setResizable(false); //prevent frame from being resized
		
		
		ImageIcon image = new ImageIcon("E:/eclipse-workspace/GUI-REVIEW/src/icon.png"); 
		frame.setIconImage(image.getImage()); //change icon of frame 
		frame.getContentPane().setBackground(new Color(0xFFFF12)); // change color of background
		
		frame.setVisible(true);//make frame visible
		*/
	myJFrame frame = new myJFrame();
	}// end of main

}
